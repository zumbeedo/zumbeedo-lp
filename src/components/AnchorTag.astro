---
interface Props {
  text: string;
  link: string;
}

const { text, link } = Astro.props;
---

<li>
  <a class="text-white hover:underline active:opacity-50" href={link}
    >{text[0].toUpperCase()}{text.slice(1)}</a
  >
</li>
